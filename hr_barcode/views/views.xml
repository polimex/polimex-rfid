<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="hr_barcode.list">
      <field name="name">hr_barcode list</field>
      <field name="model">hr_barcode.hr_barcode</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="hr_barcode.action_window">
      <field name="name">hr_barcode window</field>
      <field name="res_model">hr_barcode.hr_barcode</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="hr_barcode.action_server">
      <field name="name">hr_barcode server</field>
      <field name="model_id" ref="model_hr_barcode_hr_barcode"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="hr_barcode" id="hr_barcode.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="hr_barcode.menu_1" parent="hr_barcode.menu_root"/>
    <menuitem name="Menu 2" id="hr_barcode.menu_2" parent="hr_barcode.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="hr_barcode.menu_1_list" parent="hr_barcode.menu_1"
              action="hr_barcode.action_window"/>
    <menuitem name="Server to list" id="hr_barcode" parent="hr_barcode.menu_2"
              action="hr_barcode.action_server"/>
    -->
  </data>
</odoo>