<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hr_attendance_theoretical_view_filter" model="ir.ui.view">
        <field name="model">hr.attendance.theoretical.time.report</field>
        <field name="arch" type="xml">
            <search>
                <field name="employee_id"/>
                <filter name="today" string="Today" domain="[('date', '=', datetime.date.today().strftime('%Y-%m-%d'))]" />
                <filter name="previous_month" string="Previous Month" domain="[('date', '&gt;=', (datetime.date.today() - relativedelta(months=1)).strftime('%Y-%m-01')), ('date', '&lt;', datetime.date.today().strftime('%Y-%m-01'))]" />
                <filter name="current_month" string="Current Month" domain="[('date', '&gt;=', datetime.date.today().strftime('%Y-%m-01'))]" />
                <filter name="current_year" string="Current Year" domain="[('date', '&gt;=', datetime.date.today().strftime('%Y-01-01'))]" />
                <separator/>
                <filter name="my" string="My Attendances" domain="[('employee_id.user_id.id', '=', uid)]" />
            </search>
        </field>
    </record>

    <record id="hr_attendance_theoretical_view_pivot" model="ir.ui.view">
        <field name="model">hr.attendance.theoretical.time.report</field>
        <field name="arch" type="xml">
            <pivot disable_linking="1">
                <field name="employee_id" type="row"/>
                <field name="date" type="col"/>
                <field name="worked_hours" type="measure" widget="float_time"/>
                <field name="theoretical_hours" type="measure" widget="float_time"/>
                <field name="difference" type="measure" widget="float_time"/>
            </pivot>
        </field>
    </record>

    <record id="hr_attendance_theoretical_view_graph" model="ir.ui.view">
        <field name="model">hr.attendance.theoretical.time.report</field>
        <field name="arch" type="xml">
            <graph>
                <field name="employee_id"/>
                <field name="date"/>
                <field name="theoretical_hours" type="measure"/>
            </graph>
        </field>
    </record>

    <record id="hr_attendance_theoretical_action" model="ir.actions.act_window">
        <field name="name">Theoretical vs Attended Time Analysis</field>
        <field name="res_model">hr.attendance.theoretical.time.report</field>
        <field name="view_type">form</field>
        <field name="context">{'search_default_previous_month': 1, 'search_default_current_month': 1, 'search_default_my': 1}</field>
        <field name="view_mode">pivot,graph</field>
    </record>

    <record id="hr_attendance_export_report_view" model="ir.ui.view">
        <field name="name">Export Attendances</field>
        <field name="model">hr.attendance.export.wizard</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form string="Export Attendances">
                <field invisible="1" name="state" />
                <field name="name" invisible="1"/>
                <field name="no_content" invisible="1"/>
                <group  states="choose">
                    <group string="Date Settings">
                        <field name="period"/>
                    </group>
                    <br/>
                    <group string="Custom Date" attrs="{'invisible': ['!', ('period', '=', 'custom_date')]}">
                        <field name="from_date" />
                        <field name="to_date" />
                    </group>
                </group>

                <group  states="choose" string="Document Settings">
                    <field name="export_format" />
                </group>

                <div states="get">
                    Download: <field name="data" readonly="1" filename="name"/>
                    <field name="no_content" readonly="1" />
                </div>

                <footer states="choose">
                    <button string="Export" name="export_hr_attendances" type="object" class="oe_link oe_right" />
                    <button string="Cancel" class="oe_link" special="cancel" />
                </footer>
                <footer states="get">
                    <button special="cancel" string="Close" type="object" />
                </footer>
            </form>
        </field>
    </record>

    <record id="hr_attendance_export_report_action" model="ir.actions.act_window">
        <field name="name">Export Attendances</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hr.attendance.export.wizard</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_hr_attendance_report"
              name="Attendances Analysis"
              parent="hr_attendance.menu_hr_attendance_report"
              sequence="10"
              groups="hr_attendance.group_hr_attendance_user"
              action="hr_attendance.hr_attendance_action_graph"
    />

    <menuitem id="menu_hr_attendance_theoretical_report"
              name="Theoretical vs Attended Time"
              action="hr_attendance_theoretical_action"
              parent="hr_attendance.menu_hr_attendance_report"
              groups="hr_attendance.group_hr_attendance"
              sequence="20"
    />

    <menuitem id="menu_hr_attendance_export_report"
        name="Export Attendances"
        action="hr_attendance_export_report_action"
        parent="hr_attendance.menu_hr_attendance_report"
        sequence="30"
        />

</odoo>
